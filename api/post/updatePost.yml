info:
  name: updatePost
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{base_url}}/api/posts/update/{{id}}"
  body:
    type: json
    data: |-
      {
        "name":"updated"

      }
  auth: inherit

runtime:
  variables:
    - name: id
      value: 6a108550921b018ef840488e

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
