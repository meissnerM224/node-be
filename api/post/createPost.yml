info:
  name: createPost
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/api/posts/create"
  body:
    type: json
    data: |-
      {
        "name":"post2",
        "description": "My second post",
        "age":1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
